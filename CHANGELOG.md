# Changelog

## 2026-06-26

- Added missing agent-readiness artifacts to satisfy low AgentLint dimensions:
  - Added `INDEX.md` for root discoverability.
  - Added `CHANGELOG.md` and `HANDOFF.md` for continuity evidence.
  - Added `plans/README.md` to preserve ongoing plan context.
  - Expanded `AGENTS.md` with explicit project description, local test section,
    explicit constraints, and rule specificity.
  - Added `test-required` workflow guard for feat/fix + test coverage checks.
