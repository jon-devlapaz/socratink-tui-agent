# Orchestration

1. Diagnose live persona findings against authoritative event and awaiting state.
2. Patch post-bridge transfer continuation before UI polish because it affects event truth.
3. Patch CTA enrichment to prevent repair prompt leakage into transfer, spacing, and idle.
4. Keep dashboard read-only and learner-loop framed; update tests to lock payload shape.
5. Run narrow tests; if a check fails, classify failure before patching again.
